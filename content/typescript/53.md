# Function in interface

```typescript
interface MariaSqlConnectionConfig {
  expirationChecker?(): boolean;
}
```

This interface describes an object that optionally includes a function called expirationChecker,
which:
- takes no parameters
- returns true or false

```typescript
(): boolean
```

This tells you that:
- expirationChecker is a function (method)
- It takes no arguments
- It returns a boolean (true or false)

